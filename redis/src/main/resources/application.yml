server:
  port: 8081

spring:
  application:
    name: spring-redis

  output:
    ansi:
      enabled: always

  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
#    password: WWJ19950811
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

    timeout: 50ms

  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle.thin:@//127.0.0.1:1521/antcloud
    username: antms
    password: antms1619


mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.gree.redis

logging:
  level:
    com.gree.dao: debug
  path: classpath:/logs/
  file: logs.log