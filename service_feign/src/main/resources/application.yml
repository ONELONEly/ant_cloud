server:
  port: 8766


spring:
  application:
    name: service-feign

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: gree
      profile: dev


feign:
  hystrix:
    enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://server2:8760/eureka/


