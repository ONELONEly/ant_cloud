server:
  port: 8769

spring:
  application:
    name: gree-auth

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: gree
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
