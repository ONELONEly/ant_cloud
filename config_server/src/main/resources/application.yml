server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native #设置为本地配置 默认为git
  cloud:
    config:
      server:
        native:
          search-locations: ../spring_cloud/config_server/config
      #        git:
#          uri: https://github.com/ONELONEly/spring_cloud_config.git
#          search-paths: spring_cloud_config
#          username: 180484
#          passphrase: 960814
#      label: master




